#ifndef MUNDY_CONFIG_HPP_
#define MUNDY_CONFIG_HPP_

// Declare special variables, especially those related to optional features, TPLs, and workarounds.

// Optional packages shared by all of mundy
#cmakedefine HAVE_MUNDY_STK  // TODO(palmerb4): This only exists because we rely on stk's mpi interface for initializing and finalizing MPI.
                             // We should eventually remove this dependency by making our own MPI initialization and finalization code.

// Compiler-agnostic deprecation macros (generated by TriBITS).
// Usage:  using OldName MUNDY_DEPRECATED_MSG("use NewName") = NewName;
@MUNDY_DEPRECATED_DECLARATIONS@

#endif  // MUNDY_CONFIG_HPP_